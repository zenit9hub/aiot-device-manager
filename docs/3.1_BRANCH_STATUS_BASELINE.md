# 3.1 Branch Status Baseline (main 기준)

## 목적
- 현재 리포지토리의 브랜치 구조와 역할을 히스토리 기반으로 고정 기록한다.
- 문서 업데이트 기준 브랜치를 `main`으로 명확히 한다.

## 점검 기준
- 점검 일시: 2026-03-15 (KST)
- 점검 기준 브랜치: `main` (`3bdb3c3`)
- 점검 방식:
  - `git fetch --all --prune`
  - `git branch -a -vv`
  - `git log --graph --oneline --decorate --all`
  - `git rev-list --left-right --count`
  - `git merge-base`

## 브랜치 토폴로지 (사실)
`v1.0-main(8d058b5, 2025-10-23)`  
→ `PROD(1818cf2, 2026-01-10)`  
→ `main(3bdb3c3, 2026-01-10)`  
→ `feat/backend-2.0-working(9fedb9e, 2026-01-11)`

별도 분기:
- `origin/feat-refactoring2.0-fe-cld(4f698bc, 2026-01-08)`는 `03106b4(2026-01-03)`에서 `main` 라인과 분기됨.

## 브랜치별 상태 요약

| 브랜치 | 최신 커밋 | `main` 대비 (main ahead / branch ahead) | 판정된 역할 |
|---|---|---:|---|
| `v1.0-main` | `8d058b5` (2025-10-23) | `35 / 0` | 2.0 리팩토링 이전 백업 기준선 |
| `PROD` | `1818cf2` (2026-01-10) | `2 / 0` | 배포 기준선(현재 `main`보다 2커밋 뒤) |
| `main` | `3bdb3c3` (2026-01-10) | `0 / 0` | 2.0 리팩토링 최신 통합 기준선 |
| `feat/backend-2.0-working` | `9fedb9e` (2026-01-11) | `0 / 1` | `main` 위 백엔드 연동 확장 브랜치 |
| `origin/feat-refactoring2.0-fe-cld` | `4f698bc` (2026-01-08) | `27 / 4` | 실험/대안 구현 브랜치(미병합) |

## 태그 기준점
- `v1.0-final` → `8d058b5` (`v1.0-main` 헤드와 동일)
- `refactoring-v2.0-RC1` → `3bdb3c3` (`main` 헤드와 동일)

## 운영 원칙 (권고)
- 브랜치 운영 상태 문서의 **원본(Source of Truth)** 은 `main`에 유지한다.
- `PROD`는 배포 안정화 기준선으로 유지하고, `main`에서 검증된 변경만 반영한다.
- 실험 브랜치(`feat-refactoring2.0-fe-cld`)는 기록은 남기되, 머지 여부는 별도 검증 기준으로 판단한다.
- 백엔드 확장(`feat/backend-2.0-working`)은 `main` 대비 변경점(연동 범위, API/인프라 영향)을 문서화한 뒤 통합 여부를 결정한다.

## 참고 메모
- 점검 시 워킹트리에 untracked 파일 `과제물예시.md`가 존재했다.
- 로컬 `main`, `v1.0-main`은 원격 upstream과 동기화(`=`) 상태였다.
